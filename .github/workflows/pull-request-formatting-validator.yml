# This workflow validates basic pull request formatting requirements are met.
#
# Copyright (c) Microsoft Corporation.
# SPDX-License-Identifier: BSD-2-Clause-Patent
#

name: Validate Pull Request Formatting

on:
  pull_request_target:
    branches:
      - master
      - merge_queues
    types:
      - edited
      - opened
      - reopened
      - synchronize
  merge_group:

concurrency:
  group: pr-validation-${{ github.event.number || github.run_id }}
  cancel-in-progress: true

jobs:
  validate_pr:
    name: Validate Pull Request Formatting
    # Run real validation only for pull requests. Merge queue builds report
    # success since formatting is validated at the pull request level.
    if: ${{ github.event_name == 'merge_group' || (github.event.pull_request.draft == false && github.repository_owner == 'tianocore') }}
    runs-on: ubuntu-latest

    steps:
      - name: Generate Token
        if: ${{ github.event_name != 'merge_group' }}
        id: app-token
        uses: actions/create-github-app-token@v3
        with:
          app-id: ${{ secrets.TIANOCORE_PR_AUTOMATION_APPLICATION_ID }}
          private-key: ${{ secrets.TIANOCORE_PR_AUTOMATION_APPLICATION_PRIVATE_KEY }}

      # Reduce checkout time with sparse-checkout
      - name: Checkout repository
        if: ${{ github.event_name != 'merge_group' }}
        uses: actions/checkout@v7
        with:
          fetch-depth: 1
          sparse-checkout: |
            .github/scripts/

      - name: Setup Python
        if: ${{ github.event_name != 'merge_group' }}
        uses: actions/setup-python@v6
        with:
          python-version: '3.x'
          cache: 'pip'
          cache-dependency-path: '.github/scripts/requirements.txt'

      - name: Install PIP Modules
        if: ${{ github.event_name != 'merge_group' }}
        run: pip install -r .github/scripts/requirements.txt

      - name: Validate PR Format
        if: ${{ github.event_name != 'merge_group' }}
        id: validate
        run: python .github/scripts/validate_pr_formatting.py
        env:
          GITHUB_TOKEN: ${{ steps.app-token.outputs.token }}
          OWNER: ${{ github.repository_owner }}
          PR_NUMBER: ${{ github.event.number }}
          REPO: ${{ github.event.pull_request.base.repo.name }}

      - name: Post Validation Comment
        if: steps.validate.outputs.validation_error == 'true'
        run: python .github/scripts/validate_pr_formatting.py --post-comment
        env:
          GITHUB_TOKEN: ${{ steps.app-token.outputs.token }}
          PR_NUMBER: ${{ github.event.number }}

      - name: Check for Validation Errors
        if: steps.validate.outputs.validation_error == 'true'
        uses: actions/github-script@v9
        with:
          script: |
            core.setFailed('PR Formatting Validation Check Failed!')
